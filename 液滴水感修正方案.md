# 液滴水感修正方案：`index-wave.html`

## 1. 修改目标

当前液滴的主要问题不是水面焦散不足，而是**液滴本体的成像模型更像半透明凝胶/塑料球，而不是水滴**。

本次修改的目标：

> 在不推翻现有水面、焦散、关系网和整体视觉风格的前提下，只重构"液滴本体"的光学表现，使液滴同时具备：
>
> -   可见的背景折射
> -   Fresnel 边缘反射
> -   真实的透明/半透明关系
> -   连续的接触区
> -   柔和、环境相关的高光
> -   与水面的光学因果关系

**不要通过单纯增加透明度、蓝色、高光或阴影来解决。**

------------------------------------------------------------------------

# 2. 总体修改原则

现有系统保留：

-   `sceneColor()`
-   `causticWeb()`
-   `aperiodicWeb()`
-   水面焦散
-   水面漂移/缓涌
-   `dropCenterPx()`
-   `dropFieldGrad()`
-   `lightField()`
-   `rippleRings()`
-   `lightPool()`
-   P4 投影逻辑
-   PixiJS/WebGL 框架
-   当前液滴布点和整体构图

重点修改：

``` text
液滴本体 shader
    ↓
从“圆形区域贴一层折射颜色”
升级为
“折射 + Fresnel反射 + 环境反射 + 接触界面”的光学合成
```

------------------------------------------------------------------------

# 3. 第一优先级：加入 Fresnel

## 当前问题

现有代码虽然有 Snell 折射：

``` glsl
vec3 R = refract(vec3(0.0, 0.0, -1.0), N, 0.752);
```

但**没有真正的 Fresnel 反射模型**。

当前的：

``` glsl
float sp = pow(max(dot(N, LDIR), 0.0), 120.0);
```

只是人为制造锐利高光，不能代替 Fresnel。

## 修改要求

在液滴本体内部计算观察方向：

``` glsl
vec3 V = normalize(vec3(0.0, 0.0, 1.0));
```

然后：

``` glsl
float cosTheta = clamp(dot(N, V), 0.0, 1.0);

float F0 = 0.0204; // 空气-水界面近似值
float fresnel = F0 + (1.0 - F0) * pow(1.0 - cosTheta, 5.0);
```

得到：

``` text
液滴中心
→ Fresnel低
→ 主要看到折射

液滴边缘
→ Fresnel高
→ 反射明显增强
```

## 注意

Fresnel 不要做成很强的白色边框。

目标是：

> 边缘更有玻璃/水体的反射感，但仍然保持透明。

------------------------------------------------------------------------

# 4. 第二优先级：强化"背景被液滴折射"的可读性

这是当前最重要的问题之一。

现有代码已经有：

``` glsl
vec3 dcol = sceneColor(refr) * lightField(refr, mn, uTime);
```

但后续大量颜色、亮度处理使折射纹理变得不明显。

## 修改要求

保留：

``` glsl
vec3 dcol = sceneColor(refr);
```

但减少后续人为染色和整体压暗。

尤其检查并弱化：

``` glsl
dcol = (dcol - 0.5) * 1.28 + 0.42;
dcol *= 0.94;
dcol *= mix(1.0, 0.86, thick);
```

不要让液滴内部变成均匀蓝灰色。

## 目标

如果水面上存在一条焦散：

``` text
外部：
──────────────
```

进入液滴后应该能够明显看到：

``` text
──────╲
       ╲
        ╲────
```

即：

> 背景水面纹理在液滴内部发生位移、压缩、拉伸。

而不是：

``` text
──────────────

    [蓝灰圆球]

──────────────
```

------------------------------------------------------------------------

# 5. 第三优先级：降低 `lensDepth`

当前：

``` js
lensDepth: 3.2
```

偏激进。

先调整到：

``` js
lensDepth: 1.6 ~ 2.2
```

建议初始值：

``` js
lensDepth: 1.9
```

原则：

> 不通过极大的折射位移制造"水感"。

应该通过：

-   正确 Fresnel
-   可读的背景折射
-   曲率
-   接触区

来建立水感。

------------------------------------------------------------------------

# 6. 第四优先级：不要用整体变暗制造厚度

当前：

``` glsl
dcol *= 0.94;
```

以及：

``` glsl
dcol *= mix(1.0, 0.86, thick);
```

容易产生：

> 蓝灰色玻璃 / 果冻 / 凝胶

## 修改要求

大幅弱化或者暂时移除整体压暗。

厚度感应该来自：

``` text
曲率
+
Fresnel
+
折射
+
反射
+
边缘光学变化
```

而不是：

``` text
液滴整体变暗
```

------------------------------------------------------------------------

# 7. 第五优先级：重做高光逻辑

当前存在两个明显的人造高光：

``` glsl
float win = exp(...);
dcol += vec3(1.0) * win * 0.26 * env;
```

以及：

``` glsl
float sp = pow(max(dot(N, LDIR), 0.0), 120.0);
dcol += vec3(1.0) * sp * 0.45 * env;
```

实际画面因此出现明显的：

> "白色小 LED 点"

这更像塑料球/玻璃珠。

## 修改要求

降低这两个高光的存在感。

建议：

``` text
win 高光：明显降低
sp 高光：明显降低
```

不要完全删除，但不要让它成为液滴的主要识别特征。

最终高光应该：

> 柔和、随曲率变化、与环境相关。

而不是：

> 一个固定的白色圆点。

------------------------------------------------------------------------

# 8. 第六优先级：加入环境反射

Fresnel 必须有"反射内容"可混合。

当前没有真正的环境反射层。

可以先使用一个简化的环境反射模型，而不需要立即建立完整 HDR 环境贴图。

建议构建：

``` glsl
vec3 reflectionColor(...)
```

反射颜色可以由：

-   上方晨光
-   水面浅蓝
-   当前背景亮度
-   简单天空渐变

共同组成。

例如：

``` text
上方反射
→ 偏亮、偏灰白

侧面反射
→ 偏浅蓝

靠近水面
→ 与水面蓝色融合
```

然后：

``` glsl
vec3 opticalColor = mix(
    refractedColor,
    reflectionColor,
    fresnel
);
```

注意：

**不要把 reflectionColor 直接设成纯白。**

否则会再次变成塑料/玻璃。

------------------------------------------------------------------------

# 9. 第七优先级：液滴本体使用"折射 + 反射"的最终合成

推荐把当前液滴内部逻辑整理成以下结构：

``` glsl
// 1. 计算液滴法线
vec3 N = ...;

// 2. 观察方向
vec3 V = ...;

// 3. Fresnel
float cosTheta = clamp(dot(N, V), 0.0, 1.0);
float F0 = 0.0204;
float fresnel = F0 + (1.0 - F0) * pow(1.0 - cosTheta, 5.0);

// 4. Snell 折射
vec3 R = refract(...);

// 5. 从折射后的水面位置采样
vec3 refractedColor = sceneColor(refr);

// 6. 环境反射
vec3 reflectedColor = reflectionColor(...);

// 7. Fresnel 合成
vec3 dcol = mix(
    refractedColor,
    reflectedColor,
    fresnel
);

// 8. 非常轻的水体颜色修正
dcol *= vec3(0.98, 1.0, 1.02);
```

这是本次修改的核心。

------------------------------------------------------------------------

# 10. 第八优先级：重新检查接触区

现有代码已经有：

-   `meniscusH`
-   `dropFieldGrad()`
-   接触线
-   背光月牙
-   P4 投影

这些全部保留。

但是当前接触区主要被用于光场计算，视觉上仍然像：

> 一个圆形物体覆盖在水面上。

## 修改目标

接触区域应该表现成：

``` text
              液滴
           ╭────────╮
        ╭──╯        ╰──╮
───────╯                ╰────── 水面
          接触过渡区
```

而不是：

``` text
       ╭────────╮
───────╯        ╰───────
        圆形边界
```

因此需要让：

``` text
液滴边缘
+
meniscus
+
Fresnel
+
水面折射
+
接触阴影
```

在同一个区域连续过渡。

------------------------------------------------------------------------

# 11. 第九优先级：不要把 dome 做得过于"圆球化"

当前：

``` glsl
h = DOME_H * r * (1 - d²)²
```

并且接触线处斜率为零。

这使液滴容易产生：

> 半透明圆球 / 果冻球

的视觉印象。

## 不要求立即推翻 dome

先保留现有高度场结构。

但建议：

``` js
domeH: 0.55
```

不要继续增大。

可以先测试：

``` js
domeH: 0.40 ~ 0.50
```

建议起点：

``` js
domeH: 0.46
```

然后通过 Fresnel 和接触区补足立体感。

------------------------------------------------------------------------

# 12. 第十优先级：降低水面焦散网强度

当前水面焦散本身偏强，而且线条较长、较锐。

当前：

``` js
webAmp: 0.2
webClamp: 0.22
```

建议先测试：

``` js
webAmp: 0.08 ~ 0.12
webClamp: 0.12 ~ 0.16
```

推荐初始：

``` js
webAmp: 0.10
webClamp: 0.14
```

目的不是让水面变得没有光影。

而是：

> 给液滴内部折射留下足够的视觉空间。

------------------------------------------------------------------------

# 13. 暂时不要修改的参数

以下系统目前不应该作为主要修改对象：

``` js
dropCount
dropSizeMin
dropSizeMax
ringSpeed
ringInner
ringOuter
ringBright
ringDark
poolAmp
poolEnvGain
poolDist
poolSigma
poolSpread
```

除非后续测试发现具体问题。

尤其不要继续通过：

``` text
ringBright
poolAmp
dropShadowAmp
```

来补偿液滴水感。

------------------------------------------------------------------------

# 14. 推荐的第一轮参数

可以先直接使用这一组：

``` js
// 液滴形态
domeH: 0.46,
meniscusH: 0.035,

// 折射
refractStrength: 0.035,
lensDepth: 1.9,

// 水面焦散
webAmp: 0.10,
webClamp: 0.14,
```

Fresnel：

``` glsl
float F0 = 0.0204;
float fresnel = F0 + (1.0 - F0) * pow(1.0 - cosTheta, 5.0);
```

高光：

> 第一轮先明显降低，不要追求"闪"。

------------------------------------------------------------------------

# 15. 修改后的液滴视觉层级

最终希望达到：

``` text
第一层：
透明液滴轮廓

第二层：
液滴内部折射的水面

第三层：
边缘 Fresnel 反射

第四层：
接触区 / 弯月面

第五层：
非常柔和的局部高光

第六层：
液滴在水面形成的焦散与阴影
```

而不是现在：

``` text
蓝色半透明圆球
+
白色小高光
+
周围白色线条
+
一团阴影
```

------------------------------------------------------------------------

# 16. Agent 执行要求

请 Agent **直接修改现有 `index-wave.html`**，不要重新搭建项目。

必须遵守：

1.  不更换 PixiJS。
2.  不删除现有水面焦散系统。
3.  不删除 `sceneColor()`。
4.  不删除 `dropFieldGrad()`。
5.  不删除 `lightField()`。
6.  不删除 P1/P2/P4 系统。
7.  只重点重构液滴本体的 shader 合成。
8.  加入真正的 Fresnel。
9.  加入环境反射颜色。
10. 保留真实的 Snell 折射。
11. 减少液滴内部的整体蓝灰染色。
12. 减少白色点状高光。
13. 降低 `lensDepth`。
14. 降低水面焦散强度。
15. 不通过增加阴影或增加高光来"伪造水感"。

------------------------------------------------------------------------

# 17. 验收标准

修改完成后，不要只检查"有没有 Fresnel 代码"。

必须从实际画面判断：

### A. 液滴内部

应该能明显看到：

> 水面纹理经过液滴后发生位移/变形。

不能是一整块均匀蓝灰。

### B. 液滴边缘

应该：

> 比中心更有反射感。

但不能形成明显白色描边。

### C. 高光

应该：

> 柔和、环境相关。

不能像 LED 小圆点。

### D. 接触区

应该：

> 看起来像液滴与水面发生了真实接触。

不能像一个圆形贴片覆盖在水面上。

### E. 整体

第一眼应该更接近：

> **"一滴透明液体浮在水面上"**

而不是：

> **"一个半透明蓝色球体放在水面上"。**

------------------------------------------------------------------------

# 18. 最终判断标准

如果只能记住一句话：

> **不要继续给液滴"加水的特效"，而要让液滴本身通过 Fresnel + 折射 +
> 反射 + 接触界面产生水感。**

当前代码已经有大量正确的物理/图形学基础，本次任务不是推翻重做，而是把这些已经存在的系统真正连接成一个统一的液滴光学模型。
